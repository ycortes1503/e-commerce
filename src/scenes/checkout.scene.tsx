import { AppLayout } from '@/layouts';
import { CheckoutContainer } from '@/pods/checkout';

export const CheckoutScene = () => {
  return (
    <AppLayout>
      <CheckoutContainer />
    </AppLayout>
  );
};
